# Design decisions

Documents are split at second-level (`##`) headings because FastAPI's Markdown
documentation uses these headings for coherent topics. Each chunk retains its
source path and public documentation URL so retrieval results can be cited.
